============
Installation
============

At the command line::

    $ easy_install service_communicator

Or, if you have virtualenvwrapper installed::

    $ mkvirtualenv service_communicator
    $ pip install service_communicator
